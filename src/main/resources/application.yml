
spring:
  datasource:
    password: 48467711pp
    url: jdbc:mysql://localhost:3306/przychodniadb?serverTimezone=UTC
    username: root
    resources:
      add-mappings: true
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
